#include <stdio.h>
int main() {
    int a,b;
    printf("Enter the numbers: ");
    scanf("%d%d", &a,&b);
    if (a==b) 
        printf("Entered numbers are equal");
        else
     printf("Entered number are unequal");
    return 0;
}
